# SKILL：信用风险分群建模与评分卡策略

> 通用信用风险建模方法论——适用于银行、消费金融、互联网金融等场景的端到端风控模型构建。

---

## 01 项目初始化与数据理解

### 1.1 业务目标澄清
在开始建模前，与业务方对齐以下核心定义：
- **违约定义**：DPD 30+ / 60+ / 90+？是否包含核销、代偿？
- **观察窗口**：取多长时间的历史数据作为特征构造窗口？
- **表现窗口**：给多长的时间窗口来观察是否违约？
- **样本切分**：训练集 / 验证集 / 测试集的切分方式（时间切分 vs 随机切分）

> **关键原则**：风控场景必须按时间切分，不能用随机切分。训练集时间 < 验证集时间 < 测试集时间。

### 1.2 数据表盘点
梳理全量数据表，建立表级全景图：

| 维度 | 常见表类型 | 数据特点 |
|------|-----------|----------|
| 主表 | 申请信息表、贷款合同表 | 一笔申请一条记录，含目标变量 |
| 辅助表 | 客户画像表、地址信息表 | 静态/准静态信息，按客户关联 |
| 时间序列表 | 征信记录、还款流水、交易流水 | 一对多，需聚合为特征 |
| 外部数据表 | 征信局报告、税务记录、运营商数据 | 外部数据源，可能有独立的时间维度 |

### 1.3 关键字段识别
- **主键**：申请 ID / 客户 ID / 合同 ID 及其关联关系
- **目标变量**：违约标记字段，确认正负样本定义
- **时间字段**：申请日期、放款日期、观察截止日期
- **金额字段**：贷款金额、收入、债务等核心业务字段

### 1.4 数据质量初检
- 各表行数和唯一主键数是否匹配
- 时间字段范围是否合理
- 是否存在重复记录或明显异常值

---

## 02 客群分层识别

### 2.1 分层维度
根据业务可用数据，识别是否有天然的子群体：

| 分层标准 | 示例 | 业务含义 |
|----------|------|----------|
| 历史信用记录 | 是否有征信报告 / 内部还款记录 | "白户" vs "非白户" |
| 产品类型 | 现金贷 / 分期 / 抵押贷 | 不同产品的风险结构不同 |
| 渠道来源 | 线上 / 线下 / 合作方引流 | 渠道客群质量差异 |
| 新老客户 | 首次申请 vs 存量客户 | 老客户有内部行为数据 |

### 2.2 分群违约率对比
- 各子群的样本占比和违约率
- 检查是否存在"辛普森悖论"（各子群趋势与整体趋势相反）

### 2.3 分群建模决策判断
**不是所有分群都值得独立建模。** 满足以下条件才建议分群：

1. 子群之间的可用特征集合显著不同（如白户缺少征信特征）
2. 同一特征在不同子群中的 IV / 风险排序差异明显
3. 单一模型在某子群上的表现显著差于其他子群
4. 子群样本量足够支撑独立训练（建议 ≥ 5000 样本，坏样本 ≥ 500）

> **核心检验**：分别训练统一模型和分群模型，对比各子群的区分度提升幅度。提升 < 2% 时通常不值得增加部署复杂度。

---

## 03 缺失值处理策略

### 3.1 三级处理框架

| 级别 | 缺失率 | 处理策略 |
|------|--------|----------|
| 轻微 | < 5% | 中位数/众数填充，或直接保留缺失标记 |
| 中等 | 5% ~ 30% | 分群填充 + is_missing 标记列 |
| 严重 | > 30% | 评估业务必要性，优先考虑剔除；确实重要则降级为标记列（有/无） |

### 3.2 按字段类型的填充原则

| 字段类型 | 推荐填充值 | 原因 |
|----------|-----------|------|
| 计数类（逾期次数、申请次数） | 0 | "不存在" = "没发生过" |
| 金额类（收入、贷款金额） | 中位数或分群中位数 | 0 会严重扭曲分布 |
| 比率类（利率、逾期率） | 分群中位数 | 不宜填 0（0 利率无业务含义） |
| 日期类 | 转为天数差，缺失填 -1 | 日期 0 = 1970-01-01 会产生误导 |
| 类别型 | 单独设为 "UNK" 或 "缺失" 类 | 保留"缺失"本身作为信号 |

### 3.3 缺失标记列的使用
- 样本量充裕（≥ 10 万）时，建议为关键字段生成 `is_xxx_missing` 标记
- 树模型能利用缺失本身作为风险信号（"未提供收入信息"可能本身就是风险）
- 逻辑回归等线性模型不宜使用过多标记列

---

## 04 特征工程（按数据域分类）

### 4.1 信用历史域
适用于有征信/内部还款记录的客户。

**逾期行为聚合**：
- 当前逾期金额 / 笔数
- 历史最大逾期天数（DPD）
- 不同逾期等级占比（30+/60+/90+）
- 近期 vs 远期逾期对比（恶化趋势指标）

**还款行为统计**：
- 提前还款比例
- 还款金额变异系数（稳定性指标）
- 账户状态流转（正常 → 关注 → 不良）

**账户/合同维度**：
- 活跃账户数 / 已关闭账户数
- 平均账龄 / 最长账龄
- 信贷类型分布（循环贷 vs 分期贷）

### 4.2 申请行为域

**申请频率**：
- 近 3/6/12 月申请次数
- 申请间隔（平均 / 最短）
- 申请产品类型多样性

**审批/拒贷信号**：
- 近 N 月拒贷次数（多机构拒贷 = 强风险信号）
- 审批通过率
- 最近一次拒贷原因

**多头借贷**：
- 同时存在的活跃合同数
- 不同机构类型（银行 / 消金 / P2P）的申请分布

### 4.3 收入/税务域

**收入水平**：
- 自报收入（注意数据质量，可能虚报）
- 多来源收入取最新 / 最高 / 中位数

**收入稳定性**：
- 历史收入变异系数
- 收入趋势（递增 vs 递减）
- 税务缴纳记录数（正规就业代理变量）

**偿债能力指标**：
- DTI（债务收入比）
- 月还款 / 月收入比
- 本次申请月供 / 剩余可支配收入

### 4.4 人口画像域

| 特征 | 构建方式 | 业务含义 |
|------|----------|----------|
| 年龄 | 出生日期 → 申请时年龄，或直接分箱 | 年轻 = 收入不稳定，年老 = 健康/收入下降风险 |
| 婚姻状态 | 已婚/单身/离异/丧偶 | 已婚 = 家庭约束 → 违约意愿低 |
| 教育水平 | 分箱或 WOE 编码 | 教育 → 收入预期 → 还款能力 |
| 职业/行业 | WOE 编码或分群 | 某些行业违约率天然高于其他 |
| 居住地 | 区域级 WOE 编码 | 区域经济水平 → 信用风险映射 |
| 居住稳定性 | 居住年限 / 搬家频率 | 频繁搬家 = 生活不稳定 |

### 4.5 构造特征原则
1. **业务可解释优先**：每个构造特征必须能用业务语言描述
2. **避免数据泄露**：不能使用申请时间之后的信息
3. **注意样本量**：特征数 / 坏样本数 < 1:10（逻辑回归）、< 1:5（树模型）
4. **先验假设检验**：构造前先想好"这个特征在什么场景下会失效"

---

## 05 特征筛选体系

### 5.1 IV（信息量）筛选
- 计算每个特征的 IV 值
- **阈值设定原则**：
  - 样本量充裕（≥ 50 万）→ IV > 0.02
  - 样本量中等（5 万~50 万）→ IV > 0.05
  - 样本量较少（< 5 万）→ IV > 0.10
- IV > 0.5 时警惕数据泄露（可能使用了未来信息）

### 5.2 PSI（稳定性）筛选
- 按时间窗口切分训练集，计算每个特征的 PSI
- 常用阈值：PSI < 0.1（稳定）、0.1~0.25（需关注）、> 0.25（建议剔除）
- **金额类、利率类字段优先监控**——最容易受宏观经济波动影响

### 5.3 相关性去重
- 计算特征间相关系数矩阵
- 对相关系数 > 0.7 的特征组，保留 IV 最高的
- 注意：高相关但业务含义互补的特征可同时保留（如"逾期次数"和"逾期金额"）

### 5.4 业务可解释性终审
- 高 IV 但业务逻辑无法解释的特征 → 审慎使用或剔除
- 零方差 / 近零方差特征 → 直接剔除
- 特征数量上限：逻辑回归 15~30 个、树模型 50~200 个

---

## 06 模型选型与训练

### 6.1 候选模型池

| 模型 | 优势 | 劣势 | 适用场景 |
|------|------|------|----------|
| **XGBoost/LightGBM** | 高区分度，自动捕捉非线性交互 | 可解释性弱于线性模型 | 复杂风控场景，特征数 > 50 |
| **LogisticRegression** | 可解释性强，监管友好 | 需手工特征工程，区分度上限低 | 监管严格场景，特征数 < 30 |
| **DecisionTree** | 完全可解释，规则透明 | 容易过拟合，单一树区分度低 | 规则引擎，或作为 baseline |

### 6.2 分群建模原则
- 根据各子群样本量**独立设定超参**：
  - 大样本子群（≥ 50 万）：max_depth=4~6, learning_rate=0.05, subsample=0.7
  - 小样本子群（< 5 万）：max_depth=2~3, reg_lambda 加大, min_child_weight 提高
- **时间序列交叉验证**：按时间窗口切分，不使用随机 K-Fold
- 部署架构：各子群模型独立部署，按客户类型路由

### 6.3 交叉组合评估方法

当存在多个客群（如 A 群 / B 群）且各群有多个候选模型时，采用全量矩阵交叉评估：

#### 6.3.1 构建组合矩阵

| | B 群模型 1 | B 群模型 2 | B 群模型 3 |
|------|-----------|-----------|-----------|
| **A 群模型 1** | 组合 1-1 | 组合 1-2 | 组合 1-3 |
| **A 群模型 2** | 组合 2-1 | 组合 2-2 | 组合 2-3 |
| **A 群模型 3** | 组合 3-1 | 组合 3-2 | 组合 3-3 |

共 M×N 种组合，所有组合在**同一份全量测试集**上评估。

#### 6.3.2 三视角评估

**视角一：子群 Solo 表现**
- 各模型仅在对应客群上评估
- 目标：确认每个子模型自身的区分度足够
- 指标：各自客群的 AUC / KS

**视角二：全量 Combined 表现**
- 将子模型预测结果合并，在全量测试集上评估
- 目标：找到全量指标最优的组合
- 指标：全量 AUC / KS / Gini

**视角三：跨时间稳定性**
- 每个组合按时间窗口切分评估
- 目标：排除"某个窗口运气好"的假象
- 指标：各窗口指标的标准差

#### 6.3.3 避免"统计假象"

某组合可能存在"全量指标最高但某子群表现很差"的情况。例如：
- A 群 XGBoost（AUC=0.85）+ B 群 DecisionTree（AUC=0.73）
- 全量 AUC 看起来不错，但 B 群客户实际上被随机对待

**判断原则**：
1. 子群 Solo 表现是组合健壮性的**底线**
2. 优先选择"两个子群各自最优"的组合，而非"全量表面最优"
3. 若子群最优模型与全量最优模型不一致，选子群最优

#### 6.3.4 最终选型

综合评分公式示例：
```
综合得分 = w1 × Mean(Gini) − w2 × Std(Gini) + w3 × 子群最低AUC
```
- Mean(Gini)：跨时间窗口的平均区分度
- Std(Gini)：跨时间窗口的稳定性惩罚
- 子群最低 AUC：确保没有客群被"放弃"

### 6.4 评估指标

| 指标 | 含义 | 阈值参考 |
|------|------|----------|
| AUC | 模型排序能力 | > 0.75 可用，> 0.80 良好，> 0.85 优秀 |
| KS | 好坏样本分离度 | > 0.30 可用，> 0.40 良好 |
| Gini | = 2×AUC − 1，行业标准 | > 0.50 可用 |
| PSI | 模型分数稳定性 | < 0.10 稳定，0.10~0.25 关注，> 0.25 告警 |

---

## 07 评分卡构建

### 7.1 概率 → 分数映射
标准评分卡公式：
```
Score = Base_Score − (PDO / ln(2)) × ln(odds)
```

其中：
- **Base_Score**：基准分，通常设为 600 或 650
- **PDO（Points to Double the Odds）**：odds 翻倍时分数下降的幅度，通常设为 20
- **odds** = p/(1−p)，即好坏比

### 7.2 参数设定原则

| 参数 | 建议范围 | 说明 |
|------|----------|------|
| Base Score | 600~700 | 分数不宜过高或过低，避免客户误解 |
| PDO | 20~50 | PDO 太小 = 分数太敏感，PDO 太大 = 区分度不够 |

### 7.3 评分卡质量检验
- **单调性**：分数越高 → 坏客率越低（必须满足）
- **区分度**：不同评分区间的坏客率应有明显梯度
- **对齐性**：多群评分卡使用不同公式时，同一分数应代表相同的风险水平

### 7.4 评分区间分箱
- 按分数等频或等距分箱（10~20 箱）
- 每箱统计：样本占比、坏客率、累计通过率
- 输出评分-坏客率对照表供业务使用

---

## 08 阈值策略与业务应用

### 8.1 通过率 vs 风险降幅权衡
- 对每个候选阈值，计算：通过率、坏客率、风险降幅（相对于全量通过）
- 画出通过率—坏客率—风险降幅的权衡曲线
- 找到"性价比拐点"：分数再提高一档 → 通过率下降很多但风险降幅增加很少

### 8.2 分群差异化阈值
- 不同客群的模型区分度不同 → 阈值不应统一
- **低区分度客群**：适当提高拒绝率（降低通过率），或设计专属产品（小额、担保）
- **高区分度客群**：可以在较高通过率下实现有效风险分层

### 8.3 多策略方案

| 策略 | 通过率 | 坏客率 | 适用场景 |
|------|--------|--------|----------|
| 保守稳健 | 低 | 极低 | 经济下行期、资本金紧张 |
| 平衡进取 | 中 | 低 | 正常市场环境、稳健增长 |
| 获客宽松 | 高 | 中 | 抢占市场、新产品冷启动 |

### 8.4 评分应用三层体系

```
评分 → 准入决策（过/拒/人工复核）
     → 额度差异化（高风险低额度，低风险高额度）
     → 定价差异化（高风险高利率，低风险低利率）
```

- **准入**：低于 cutoff 直接拒绝，cutoff 附近人工复核
- **额度**：结合收入、DTI，按风险分层设额度上限
- **定价**：风险定价需要考虑监管利率上限

---

## 09 模型稳定性监控体系

### 9.1 时间窗口切分
- 窗口长度取决于样本量：每窗口至少 100 个坏样本才具有统计意义
- 常用窗口：月 / 4 周 / 双周
- 训练集、验证集、测试集之外，预留最近 1~2 个窗口作为 OOT（Out of Time）

### 9.2 核心监控指标

| 指标 | 监控对象 | 绿色 | 黄色 | 红色 |
|------|----------|------|------|------|
| PSI | 模型分数分布 | < 0.10 | 0.10~0.25 | > 0.25 |
| KS | 模型区分度 | 波动 < 0.03 | 0.03~0.05 | > 0.05 |
| AUC | 模型排序能力 | 波动 < 0.02 | 0.02~0.05 | > 0.05 |
| 通过率 | 业务影响 | 波动 < 2% | 2%~5% | > 5% |

### 9.3 分群监控
- **各客群分别监控**——小样本客群的指标波动天然更大，不应触发误报
- 小样本客群可适当放宽 PSI/KS 阈值
- 若某客群指标持续恶化 → 该客群独立迭代模型

### 9.4 监控节奏
- **线上日报**：通过率、拒绝率、平均分数
- **每窗口周报**：PSI、KS、AUC
- **月度深度分析**：特征漂移 top-N、分数偏移原因定位

---

## 10 可解释性与合规

### 10.1 全局解释
- **特征重要性排名**（Gain / SHAP 均值）
- 确认 TOP 特征与业务直觉一致
- 警惕"反直觉高重要特征"（可能是数据泄露或标签泄露）

### 10.2 局部解释
- SHAP Waterfall 图：单个样本的预测归因
- 典型样本分析：高风险/低风险/误判样本各取 2~3 例
- 拒绝原因自动生成：将 top-N 正向 SHAP 特征翻译为自然语言拒绝理由

### 10.3 分群解释对比
- 对比各客群的 TOP 10 特征
- 验证"不同客群的风险驱动因子不同"的假设
- 若两群 TOP 特征高度重叠 → 重新评估分群建模必要性

### 10.4 合规要求
- **可解释性**：每个决策（尤其是拒绝）必须可追溯归因
- **公平性**：检查模型是否存在性别、年龄、地域等维度的系统性偏差
- **文档留存**：模型开发报告、特征字典、评分卡映射表、阈值决策依据

---

## 附录：项目里程碑检查清单

- [ ] 01 业务目标对齐（违约定义、时间窗口、样本切分）
- [ ] 02 客群识别与分群决策（是否分群、如何分群）
- [ ] 03 缺失值处理方案记录（每个字段的处理方式和理由）
- [ ] 04 特征工程完成（四大域特征 + 构造特征）
- [ ] 05 特征筛选完成（IV + PSI + 相关性 + 可解释性）
- [ ] 06 模型训练完成（交叉组合评估 + 选型报告）
- [ ] 07 评分卡构建（公式 + 分箱 + 单调性检验）
- [ ] 08 阈值策略 + 业务应用方案（准入/额度/定价）
- [ ] 09 稳定性监控方案（指标体系 + 阈值 + 频率）
- [ ] 10 可解释性报告 + 合规文档
